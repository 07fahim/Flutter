import 'package:flutter/material.dart';

class Homeframents extends StatelessWidget {
  const Homeframents({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Home Screen")),
    );
  }
}
