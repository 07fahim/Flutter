import 'package:flutter/material.dart';

class Balanceframents extends StatelessWidget {
  const Balanceframents({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Balance Screen")),
    );
  }
}
