import 'package:flutter/material.dart';

class Personframent extends StatelessWidget {
  const Personframent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Person Screen")),
    );
  }
}
