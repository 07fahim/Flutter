import 'package:flutter/material.dart';

class Emailfragments extends StatelessWidget {
  const Emailfragments({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Email Screen")),
    );
  }
}
