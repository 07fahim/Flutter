import 'package:flutter/material.dart';

class Settingsframent extends StatelessWidget {
  const Settingsframent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Settings Screen")),
    );
  }
}
