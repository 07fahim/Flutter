import 'package:flutter/material.dart';

class Contactsframents extends StatelessWidget {
  const Contactsframents({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Contacts Screen")),
    );
  }
}
